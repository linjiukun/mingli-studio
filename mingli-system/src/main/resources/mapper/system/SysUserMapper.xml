<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mingli.system.mapper.SysUserMapper">

    <resultMap id="SysUserResult" type="com.mingli.system.domain.SysUser">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="nickname" column="nickname"/>
        <result property="realName" column="real_name"/>
        <result property="gender" column="gender"/>
        <result property="birthDate" column="birth_date"/>
        <result property="birthHour" column="birth_hour"/>
        <result property="birthPlace" column="birth_place"/>
        <result property="email" column="email"/>
        <result property="avatar" column="avatar"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="selectUserVo">
        SELECT id, username, password, nickname, real_name, gender, birth_date, birth_hour,
               birth_place, email, avatar, status, create_time, update_time
        FROM sys_user
    </sql>

    <select id="selectById" parameterType="Long" resultMap="SysUserResult">
        <include refid="selectUserVo"/>
        WHERE id = #{id}
    </select>

    <select id="selectByUsername" parameterType="String" resultMap="SysUserResult">
        <include refid="selectUserVo"/>
        WHERE username = #{username}
    </select>

    <select id="selectList" parameterType="com.mingli.system.domain.SysUser" resultMap="SysUserResult">
        <include refid="selectUserVo"/>
        <where>
            <if test="username != null and username != ''">
                AND username LIKE CONCAT('%', #{username}, '%')
            </if>
            <if test="nickname != null and nickname != ''">
                AND nickname LIKE CONCAT('%', #{nickname}, '%')
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="gender != null and gender != ''">
                AND gender = #{gender}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <insert id="insert" parameterType="com.mingli.system.domain.SysUser" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user (
            username, password, nickname, real_name, gender, birth_date, birth_hour,
            birth_place, email, avatar, status, create_time, update_time
        ) VALUES (
            #{username}, #{password}, #{nickname}, #{realName}, #{gender}, #{birthDate}, #{birthHour},
            #{birthPlace}, #{email}, #{avatar}, #{status}, #{createTime}, #{updateTime}
        )
    </insert>

    <update id="update" parameterType="com.mingli.system.domain.SysUser">
        UPDATE sys_user
        <set>
            <if test="username != null and username != ''">username = #{username},</if>
            <if test="password != null and password != ''">password = #{password},</if>
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="realName != null">real_name = #{realName},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="birthDate != null">birth_date = #{birthDate},</if>
            <if test="birthHour != null">birth_hour = #{birthHour},</if>
            <if test="birthPlace != null">birth_place = #{birthPlace},</if>
            <if test="email != null">email = #{email},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="status != null">status = #{status},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="Long">
        DELETE FROM sys_user WHERE id = #{id}
    </delete>

</mapper>
